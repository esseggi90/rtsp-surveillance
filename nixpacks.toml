[phases.setup]
nixPkgs = ["libglib2-dev", "libsm6", "libxrender1", "libxext6", "libgl1-mesa-glx"]

[phases.build]
cmds = [
  "pip uninstall -y opencv-python || true",
  "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
