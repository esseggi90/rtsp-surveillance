[phases.setup]
nixPkgs = ["libGL", "glib", "libxcb", "libSM", "libXrender", "libXext"]

[phases.build]
cmds = [
  "pip uninstall -y opencv-python || true",
  "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
